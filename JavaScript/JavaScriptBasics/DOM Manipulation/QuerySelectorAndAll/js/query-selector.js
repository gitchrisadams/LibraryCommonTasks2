// querySelector only returns the first match.// So this only sets the first li.hot from hot to cool color.var el = document.querySelector('li.hot');el.className = 'cool';// querySelectorAll returns a NodeList so all li.hot are found.// Since we already changed the first li to cool, it finds the two li.hots// The third overall li element is then selected and changed. // This is index 1 because there are only 2 li hots now, index 0 and index 1.var els = document.querySelectorAll('li.hot');els[1].className = 'cool';